# KIMTECH Admission Portal — Environment Variables
# Copy this file to .env and fill in values (never commit .env to git)
#
# On Hostinger: set these in hPanel → Advanced → PHP Configuration → Environment Variables
# OR place a .env file in the web root (blocked from public access via .htaccess)

# ── Application ──────────────────────────────────────────────────
APP_ENV=production

# ── Encryption key for secrets (AES-256-GCM) ─────────────────────
# Generate with: php -r "echo base64_encode(random_bytes(32));"
KIMTECH_SECRET_KEY=

# ── Database (production only — alternative to db_prod.php) ──────
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=your_database
# DB_USER=your_user
# DB_PASS=your_password

# ── Cron alert email ─────────────────────────────────────────────
# Receives an email if the SMS cron job crashes
CRON_ALERT_EMAIL=admin@yourdomain.com
